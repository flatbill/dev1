[build]
  functions = "functions"
  # This is the directory is publishing to netlify's CDN
  #publish = "build"

[dev]
  # Local dev command. A.k.a npm start
  # command = "react-scripts start"
  
  external_node_modules = ["@supabase"]
